{
  "name": "react-playground",
  "version": "0.0.1",
  "description": "React + Typescript playground",
  "scripts": {
    "build": "esbuild src/index.tsx --bundle --sourcemap --inject:src/react-shim.ts --outfile=public/dist/index.js --watch"
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.14.45",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
